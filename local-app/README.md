# pr1nt local app

This Electron wrapper starts the bundled Rust local server and opens the local
admin UI in a desktop window.

## Development

```bash
npm install
npm run pack
```

The packaged app is generated under the local build output directory. Release
artifacts are not committed to this repository.
